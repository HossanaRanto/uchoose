import "package:flutter/material.dart";

class VoteFragment extends StatelessWidget {
  const VoteFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Vote"),
    );
  }
}
